import { useState } from "react";
import { PostCreateModal, Sidebar } from "../Components";

export const TestPage = () => {
  return (
    <div className="w-[20%]">
      <Sidebar />
    </div>
  );
};
